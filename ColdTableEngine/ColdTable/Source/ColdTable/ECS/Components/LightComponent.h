#pragma once
#include "Component.h"

namespace ColdTable
{

	/*
	class LightComponent : public Component
	{
	public:
		LightComponent(GameObjectPtr Owner, LightSource);

		ComponentPtr Clone(GameObjectPtr newOwner) const override;
		void DrawToUI() override;

		MeshPtr Mesh;
	};

	using LightComponentPtr = std::shared_ptr<LightComponent>;*/
}

